{{- if .Values.powershellService.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "autoaudit.fullname" . }}-powershell-service
  labels:
    {{- include "autoaudit.componentLabels" (dict "context" . "component" "powershell-service") | nindent 4 }}
spec:
  {{- if .Values.powershellService.podDisruptionBudget.minAvailable }}
  minAvailable: {{ .Values.powershellService.podDisruptionBudget.minAvailable }}
  {{- else }}
  maxUnavailable: {{ default 1 .Values.powershellService.podDisruptionBudget.maxUnavailable }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "autoaudit.selectorLabels" (dict "context" . "component" "powershell-service") | nindent 6 }}
{{- end }}
